{
  "routes": [
    {
      "method": "GET",
      "path": "/bars",
      "handler": "bars.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/bars/count",
      "handler": "bars.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/bars/:id",
      "handler": "bars.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/bars",
      "handler": "bars.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/bars/:id",
      "handler": "bars.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/bars/:id",
      "handler": "bars.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
